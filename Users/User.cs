namespace Users
{
    public record User(int Id,
                   string Name,
                   string ProfilePicture,
                   string Username,
                   string connectionId);
}
